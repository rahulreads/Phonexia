#ifndef -USERPLAN_H
#define -USERPLAN_H


class -UserPlan
{
    public:
        -UserPlan();
        virtual ~-UserPlan();
        -UserPlan(const -UserPlan& other);
        -UserPlan& operator=(const -UserPlan& other);

    protected:

    private:
};

#endif // -USERPLAN_H
